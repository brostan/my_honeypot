# 🎯 НАЧНИТЕ ЗДЕСЬ

## Добро пожаловать в проект Cowrie Honeypot!

Этот проект полностью настроен и готов к использованию. Следуйте этому простому руководству.

---

## 📋 Что у вас есть

✅ **Cowrie SSH/Telnet Honeypot** - установлен и настроен  
✅ **Docker конфигурация** - для простого развертывания  
✅ **Скрипты автоматизации** - для анализа и мониторинга  
✅ **Полная документация** - на русском языке  
✅ **Готовые конфигурации** - для production использования  

---

## 🚀 Выберите способ запуска

### Вариант 1: Быстрое тестирование (1 минута)

Для локального тестирования на вашем Mac:

```bash
cd cowrie
./cowrie-env/bin/twistd -n cowrie
```

В другом терминале:
```bash
ssh -p 2222 root@localhost
# Пароль: любой (например: root, 12345678, admin)
```

Остановка: `Ctrl+C`

---

### Вариант 2: Docker (2 минуты) 🐳

Самый простой способ для продолжительного использования:

```bash
# Запуск
docker-compose -f docker-compose.simple.yml up -d

# Проверка
docker-compose -f docker-compose.simple.yml ps

# Подключение
ssh -p 2222 root@localhost

# Логи
docker-compose -f docker-compose.simple.yml logs -f cowrie

# Остановка
docker-compose -f docker-compose.simple.yml down
```

---

### Вариант 3: Полный стек с мониторингом (5 минут) 📊

Включает Kibana, Grafana, Elasticsearch для визуализации:

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Доступные интерфейсы:
# - SSH: ssh -p 2222 root@localhost
# - Telnet: telnet localhost 2223
# - Kibana: http://localhost:5601
# - Grafana: http://localhost:3000 (admin/admin)
# - Portainer: https://localhost:9443
```

---

### Вариант 4: Развертывание на сервере (10 минут) 🖥️

Для установки на Ubuntu/Debian сервер:

```bash
# 1. Скопируйте проект на сервер
scp -r Honeypot user@your-server:/home/user/

# 2. Подключитесь к серверу
ssh user@your-server

# 3. Запустите автоматическую установку
cd /home/user/Honeypot
chmod +x deploy.sh
./deploy.sh

# Скрипт все сделает автоматически!
```

---

## 📚 Документация

| Файл | Когда читать | Описание |
|------|--------------|----------|
| **[QUICKSTART.md](QUICKSTART.md)** | 🟢 Сейчас | Быстрый старт за 5 минут |
| **[README.md](README.md)** | 🟡 После запуска | Полное описание проекта |
| **[PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)** | 🟡 После запуска | Обзор всего проекта |
| **[MONITORING.md](MONITORING.md)** | 🟠 Через день | Мониторинг и анализ |
| **[SECURITY.md](SECURITY.md)** | 🔴 Перед production | Критически важно! |

---

## 🎓 Что дальше?

### Сразу после запуска

1. **Проверьте, что работает**
   ```bash
   ssh -p 2222 root@localhost
   # Попробуйте разные пароли
   ```

2. **Посмотрите логи**
   ```bash
   # Текстовые
   tail -f cowrie/var/log/cowrie/cowrie.log
   
   # JSON (структурированные)
   tail -f cowrie/var/log/cowrie/cowrie.json | jq .
   ```

3. **Запустите мониторинг**
   ```bash
   ./scripts/monitor.sh
   ```

### В первый час

1. **Подключитесь несколько раз**
   - Попробуйте разные пароли
   - Выполните команды: `ls`, `pwd`, `whoami`, `cat /etc/passwd`
   - Попробуйте `wget` или `curl`

2. **Посмотрите результаты**
   ```bash
   ./scripts/analyze.sh
   ```

3. **Воспроизведите сессию**
   ```bash
   ls cowrie/var/lib/cowrie/tty/
   ./cowrie/bin/playlog cowrie/var/lib/cowrie/tty/<имя-файла>.log
   ```

### В первый день

1. Настройте визуализацию (если используете полный стек)
2. Изучите [MONITORING.md](MONITORING.md)
3. Попробуйте разные запросы для анализа логов
4. Настройте автоматический бэкап

### Перед production

1. **ОБЯЗАТЕЛЬНО** прочитайте [SECURITY.md](SECURITY.md)
2. Настройте изоляцию сети
3. Настройте firewall правила
4. Настройте алерты
5. Протестируйте все функции

---

## 🛠️ Полезные команды

### Анализ данных

```bash
# Топ IP-адресов атакующих
jq -r 'select(.eventid=="cowrie.session.connect") | .src_ip' \
  cowrie/var/log/cowrie/cowrie.json | sort | uniq -c | sort -rn | head -10

# Топ паролей
jq -r 'select(.eventid=="cowrie.login.success") | .password' \
  cowrie/var/log/cowrie/cowrie.json | sort | uniq -c | sort -rn | head -10

# Топ команд
jq -r 'select(.eventid=="cowrie.command.input") | .input' \
  cowrie/var/log/cowrie/cowrie.json | sort | uniq -c | sort -rn | head -10
```

### Управление

```bash
# Статистика
./scripts/analyze.sh

# Мониторинг в реальном времени
./scripts/monitor.sh

# Резервное копирование
./scripts/backup.sh
```

---

## ⚠️ Важные напоминания

### 🔴 Безопасность

- **Изолируйте honeypot** от production сетей
- **Блокируйте исходящий трафик** (кроме необходимого)
- **Мониторьте активность** постоянно
- **Читайте SECURITY.md** перед production развертыванием

### 🟡 Обслуживание

- Проверяйте логи регулярно
- Делайте бэкапы еженедельно
- Обновляйте систему ежемесячно
- Анализируйте тренды

### 🟢 Лучшие практики

- Документируйте интересные находки
- Делитесь данными с сообществом (anonymized)
- Используйте для обучения команды
- Интегрируйте с вашей SIEM

---

## 🆘 Помощь

### Проблемы с запуском?

1. Проверьте логи:
   ```bash
   tail -f cowrie/var/log/cowrie/cowrie.log
   ```

2. Проверьте, что порт свободен:
   ```bash
   lsof -i :2222
   ```

3. Для Docker:
   ```bash
   docker-compose logs cowrie
   ```

### Нужна помощь?

- 📖 Читайте документацию в этой папке
- 💬 [Cowrie Slack](https://www.cowrie.org/slack/)
- 🐛 [GitHub Issues](https://github.com/cowrie/cowrie/issues)
- 📚 [Официальная документация](https://docs.cowrie.org/)

---

## 🎉 Готово!

Ваш honeypot настроен и готов к работе!

**Следующий шаг**: Откройте [QUICKSTART.md](QUICKSTART.md) для детальных инструкций.

---

## 📊 Структура проекта

```
Honeypot/
├── START_HERE.md           ← Вы здесь!
├── QUICKSTART.md           ← Читайте следующим
├── README.md               ← Полная документация
├── PROJECT_SUMMARY.md      ← Обзор проекта
├── MONITORING.md           ← Мониторинг и анализ
├── SECURITY.md             ← Безопасность (важно!)
│
├── deploy.sh               ← Автоматическое развертывание
├── docker-compose.yml      ← Полный стек
├── docker-compose.simple.yml ← Только Cowrie
│
├── cowrie/                 ← Honeypot
│   ├── etc/
│   │   ├── cowrie.cfg     ← Конфигурация
│   │   └── userdb.txt     ← Учетные данные
│   └── var/
│       ├── log/           ← Логи
│       └── lib/cowrie/
│           ├── downloads/ ← Загруженные файлы
│           └── tty/       ← Записи сессий
│
└── scripts/               ← Утилиты
    ├── analyze.sh         ← Анализ
    ├── monitor.sh         ← Мониторинг
    └── backup.sh          ← Бэкап
```

---

**Удачи в изучении кибербезопасности! 🔒**

*Создано с ❤️ для образовательных целей*

