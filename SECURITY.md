# 🔒 Безопасность Cowrie Honeypot

## ⚠️ Важное предупреждение

Honeypot - это потенциально опасная система, которая **намеренно** привлекает злоумышленников. Неправильная настройка может привести к:
- Компрометации других систем в вашей сети
- Использованию вашего сервера для атак на третьи стороны
- Юридическим проблемам
- Утечке конфиденциальной информации

## 🛡️ Принципы безопасности

### 1. Изоляция

#### Сетевая изоляция

**Обязательно:**
- Размещайте honeypot в отдельной VLAN/подсети
- Используйте DMZ (демилитаризованную зону)
- Никогда не размещайте в production сети

**Рекомендуемая топология:**

```
Internet
    │
    ├─── Firewall ───┐
    │                │
    │           [DMZ/Honeypot]
    │                │
    │                └─── Cowrie (изолирован)
    │
    └─── Firewall ───┐
                     │
                [Production Network]
                     │
                     └─── Ваши реальные серверы
```

#### Правила Firewall

```bash
# Разрешить входящие подключения к honeypot
iptables -A INPUT -p tcp --dport 22 -j ACCEPT
iptables -A INPUT -p tcp --dport 23 -j ACCEPT

# Разрешить исходящие DNS (для логирования)
iptables -A OUTPUT -p udp --dport 53 -j ACCEPT

# Разрешить исходящие HTTP/HTTPS (для загрузки вредоносов)
iptables -A OUTPUT -p tcp --dport 80 -j ACCEPT
iptables -A OUTPUT -p tcp --dport 443 -j ACCEPT

# БЛОКИРОВАТЬ все остальные исходящие подключения
# Это предотвращает использование honeypot для атак
iptables -A OUTPUT -j DROP

# Логировать заблокированные попытки
iptables -A OUTPUT -j LOG --log-prefix "HONEYPOT_BLOCKED: "
```

#### Контейнеризация

Используйте Docker для дополнительной изоляции:

```yaml
# docker-compose.yml
services:
  cowrie:
    # ... другие настройки ...
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    networks:
      honeypot-network:
        ipv4_address: 172.20.0.2
```

### 2. Мониторинг исходящего трафика

**Критически важно!** Атакующие могут попытаться использовать honeypot для:
- DDoS атак
- Сканирования других сетей
- Спама
- Проксирования трафика

#### Настройка мониторинга

```bash
# Установка tcpdump для мониторинга
apt-get install tcpdump

# Мониторинг всего исходящего трафика
tcpdump -i eth0 -n 'src host HONEYPOT_IP' -w /var/log/honeypot-traffic.pcap

# Алерт на подозрительный исходящий трафик
#!/bin/bash
# /usr/local/bin/monitor-outbound.sh
while true; do
    OUTBOUND=$(netstat -an | grep ESTABLISHED | grep -v ':22\|:23' | wc -l)
    if [ $OUTBOUND -gt 5 ]; then
        echo "WARNING: Unusual outbound connections detected!" | \
            mail -s "Honeypot Alert" admin@example.com
    fi
    sleep 60
done
```

### 3. Ограничение ресурсов

#### CPU и память

```bash
# Ограничение через systemd
# /etc/systemd/system/cowrie.service
[Service]
CPUQuota=50%
MemoryLimit=512M
MemoryMax=1G
TasksMax=100
```

#### Docker limits

```yaml
services:
  cowrie:
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
```

#### Rate limiting

```ini
# etc/cowrie.cfg
[honeypot]
# Максимум подключений с одного IP
max_connections_per_ip = 10

# Максимум общих подключений
max_connections = 100

# Таймаут неактивной сессии
interactive_timeout = 180
```

### 4. Защита данных

#### Не храните чувствительную информацию

**Никогда не размещайте на honeypot:**
- Реальные пароли
- API ключи
- Сертификаты production систем
- Конфиденциальные данные
- Доступы к другим системам

#### Шифрование логов

```bash
# Шифрование бэкапов
tar -czf - cowrie/var/log | \
    gpg --encrypt --recipient admin@example.com > \
    backup-$(date +%Y%m%d).tar.gz.gpg
```

#### Безопасное хранение загруженных файлов

```bash
# Автоматическое сканирование загруженных файлов
#!/bin/bash
# /usr/local/bin/scan-downloads.sh

DOWNLOAD_DIR="/home/cowrie/cowrie/var/lib/cowrie/downloads"
QUARANTINE_DIR="/home/cowrie/quarantine"

inotifywait -m -e create "$DOWNLOAD_DIR" | while read path action file; do
    echo "New file detected: $file"
    
    # Сканирование ClamAV
    clamscan "$path$file"
    
    # Перемещение в карантин
    mv "$path$file" "$QUARANTINE_DIR/"
    
    # Отправка в VirusTotal (опционально)
    # vt scan file "$QUARANTINE_DIR/$file"
done
```

### 5. Обновления и патчи

```bash
# Регулярное обновление Cowrie
cd /home/cowrie/cowrie
sudo -u cowrie git pull
sudo -u cowrie ./cowrie-env/bin/pip install --upgrade -e .
sudo systemctl restart cowrie

# Обновление системы
apt-get update && apt-get upgrade -y

# Обновление Docker образов
docker-compose pull
docker-compose up -d
```

### 6. Аудит и логирование

#### Логирование доступа к серверу honeypot

```bash
# Настройка auditd
apt-get install auditd

# Мониторинг доступа к файлам Cowrie
auditctl -w /home/cowrie/cowrie/var/log/ -p wa -k cowrie_logs
auditctl -w /home/cowrie/cowrie/etc/ -p wa -k cowrie_config

# Просмотр аудит логов
ausearch -k cowrie_logs
```

#### Централизованное логирование

Отправляйте логи на отдельный сервер:

```bash
# rsyslog конфигурация
# /etc/rsyslog.d/50-cowrie.conf
*.* @@log-server.example.com:514
```

### 7. Обнаружение компрометации

#### Признаки компрометации honeypot

- Необычный исходящий трафик
- Изменения в конфигурационных файлах
- Новые процессы, не связанные с Cowrie
- Изменения в системных файлах
- Попытки повышения привилегий

#### Мониторинг целостности

```bash
# Установка AIDE (Advanced Intrusion Detection Environment)
apt-get install aide

# Инициализация базы данных
aideinit

# Регулярная проверка (cron)
# /etc/cron.daily/aide-check
#!/bin/bash
aide --check | mail -s "AIDE Report" admin@example.com
```

## 🚨 Процедуры реагирования

### При обнаружении компрометации

1. **Немедленно изолируйте систему**
   ```bash
   # Отключить сетевой интерфейс
   ifconfig eth0 down
   
   # Или остановить Cowrie
   systemctl stop cowrie
   ```

2. **Сохраните доказательства**
   ```bash
   # Создайте образ памяти
   dd if=/dev/mem of=/tmp/memory.dump
   
   # Сохраните логи
   tar -czf incident-$(date +%Y%m%d).tar.gz \
       /var/log/ \
       /home/cowrie/cowrie/var/
   ```

3. **Проанализируйте инцидент**
   - Определите вектор атаки
   - Оцените масштаб компрометации
   - Проверьте другие системы

4. **Восстановление**
   - Переустановите систему с нуля
   - Примените усиленные меры безопасности
   - Обновите правила firewall

### Контакты для инцидентов

Подготовьте список контактов:
- Администратор системы
- Служба безопасности
- Провайдер (для блокировки трафика)
- Юридический отдел

## 📋 Чеклист безопасности

### Перед развертыванием

- [ ] Honeypot размещен в изолированной сети
- [ ] Настроены правила firewall (блокировка исходящих)
- [ ] Установлены лимиты ресурсов
- [ ] Настроено логирование
- [ ] Настроены алерты на подозрительную активность
- [ ] Удалена вся чувствительная информация
- [ ] Настроено резервное копирование
- [ ] Документированы процедуры реагирования

### Регулярное обслуживание

- [ ] Еженедельно проверять логи
- [ ] Ежемесячно обновлять систему
- [ ] Ежеквартально проводить аудит безопасности
- [ ] Ежегодно пересматривать политики безопасности

### Мониторинг

- [ ] Алерты на исходящий трафик работают
- [ ] Мониторинг ресурсов активен
- [ ] Логи отправляются на внешний сервер
- [ ] Резервные копии создаются автоматически

## 🔐 Дополнительные меры

### Fail2ban для защиты управления

```bash
# Защита SSH доступа к серверу honeypot
apt-get install fail2ban

# /etc/fail2ban/jail.local
[sshd]
enabled = true
port = 22222  # Используйте нестандартный порт для управления
maxretry = 3
bantime = 3600
```

### VPN для удаленного доступа

Используйте VPN для доступа к интерфейсам мониторинга:

```bash
# Установка WireGuard
apt-get install wireguard

# Доступ к Kibana/Grafana только через VPN
iptables -A INPUT -p tcp --dport 5601 -s VPN_SUBNET -j ACCEPT
iptables -A INPUT -p tcp --dport 5601 -j DROP
```

### 2FA для критичных сервисов

```bash
# Google Authenticator для SSH
apt-get install libpam-google-authenticator

# Настройка в /etc/pam.d/sshd
auth required pam_google_authenticator.so
```

## 📚 Дополнительные ресурсы

- [NIST Cybersecurity Framework](https://www.nist.gov/cyberframework)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CIS Benchmarks](https://www.cisecurity.org/cis-benchmarks/)
- [Honeypot Best Practices](https://www.sans.org/white-papers/)

## ⚖️ Юридические аспекты

### Перед развертыванием

1. **Проконсультируйтесь с юристом** о законности honeypot в вашей юрисдикции
2. **Уведомите провайдера** о намерении запустить honeypot
3. **Подготовьте политику** обработки инцидентов
4. **Документируйте** все действия для возможного расследования

### Ответственность

- Вы ответственны за трафик, исходящий с вашего сервера
- Honeypot не должен использоваться для атак на третьи стороны
- Собранные данные должны обрабатываться согласно законам о защите данных

---

**Помните**: Безопасность - это процесс, а не состояние. Регулярно пересматривайте и улучшайте меры защиты.

